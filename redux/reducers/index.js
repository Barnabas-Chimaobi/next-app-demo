import userSlice, { userDetails} from './authReducer';
// import networkSlice, {checkNetwork} from './network'

export {
  userSlice,
  userDetails,
  // networkSlice,
  // checkNetwork,
  // semester,
  // session
};